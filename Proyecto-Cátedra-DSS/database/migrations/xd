use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateAccionesPersonalTable extends Migration
{
    public function up()
    {
        Schema::create('acciones_personal', function (Blueprint $table) {
            $table->id();
            $table->string('tipo_accion');
            $table->text('descripcion');
            $table->enum('estado', ['Pendiente', 'Aprobado', 'Rechazado']);
            $table->date('fecha_solicitud');
            $table->date('fecha_aprobacion')->nullable();
            $table->unsignedBigInteger('id_gerente_sucursal');
            $table->foreign('id_gerente_sucursal')->references('idEncargado')->on('sucursales');
            $table->timestamps();
        });
    }

    public function down()
    {
        Schema::dropIfExists('acciones_personal');
    }
}
